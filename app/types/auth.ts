export type AccessToken = {
    access_token: string;
    token_type: string;
  };
  
  export type CSRFToken = {
    id: string;
  };
  
  export type UserView = {
    id: number;
    email: string;
    user_name: string;
  };
  